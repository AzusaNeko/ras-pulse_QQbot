# 更新日志

## 2026-08-31

### 0.4.81

- 优化监控任务列表的滚轮行为：列表滚至顶部或底部后，继续滚动会切换为外层页面滚动。

### 0.4.80

- 修复 Windows 系统托盘图标显示为空白的问题：改用内嵌 PNG 位图，避免 Windows 通知区域无法解码 SVG。
- 继续优化监控任务卡片响应式布局：任务列表随窗口宽度扩展；删除按钮固定为右上角的小型 ×，不再随操作按钮换行。

### 0.4.79

- 优化 Windows 系统托盘图标：改为高对比度的 Mercari Pulse「M」标识，启动时最小化或后台运行时不再显示空白图标。

### 0.4.78

- 优化监控任务卡片布局：操作区根据窗口宽度自动换行，窄窗口下“收起详情”会收缩为图标，避免与任务内容互相挤压。
- 偏好设置新增“开机自动启动”：开启后会注册 Windows 登录项；若同时启用“启动时最小化”，开机后会直接驻留系统托盘。

### 0.4.77

- “追加同步初始结果”不再清除该关键词已有商品动态，只追加未出现过的最新初始卡片并按商品 ID 去重。
- 商品卡片分别展示“Mercari 搜索排序参考时间”（Mercari 返回更新时间）与“原始上架时间”，便于识别卖家编辑后重新进入搜索前列的旧商品。

## 2026-08-30

### 0.4.76

- 商品动态存储改为“关键词分类”和“全部”视图相互独立：每个关键词最多保留 200 条；“全部”仅展示最新 300 条，超过时不会删除任何关键词分类中的卡片。

### 0.4.75

- 优化首次商品同步：首次空结果不再被立即视为完成，会以短间隔自动重试最多 3 次；后续首次拿到的商品仍作为“初始结果”展示，不触发 Windows 或 QQ 推送。
- 监控任务卡片新增“初始同步中”状态与运行说明，日志会记录搜索返回数量、重试次数和最终展示数。
- 每个监控任务新增“重新同步初始结果”按钮，可手动重新获取首次展示商品；操作只更新商品动态，不会补发通知。

### 0.4.74

- “我的收藏”新增“通过链接添加收藏”入口：可粘贴日本 Mercari 普通商品链接、Mercari Shops 商品链接，或以 `m` 开头的商品 ID。
- 添加前会校验链接域名、路径与商品详情；非 Mercari 链接、格式错误、失效或不存在的商品不会加入收藏，并会显示原因。

## 2026-08-28

### 0.4.73

- 优化监控任务拖拽排序的动态反馈：拖动时显示整张任务卡片预览，原卡片淡出；悬停目标位置会出现高亮插入线和位移动效。

### 0.4.72

- 监控任务卡片新增拖动把手，可自由拖放到另一任务卡片前以调整显示顺序；调整结果会持久化保存，重启软件后仍保持。

### 0.4.71

- 软件重启后的首次同步不再直接丢弃离线期间出现、且此前未见过的商品：会补充至“商品动态”并标记为“离线期间上新”。这些商品不会触发 Windows 弹窗、声音或 QQ 机器人推送。

### 0.4.70

- 监控任务“统一管理”新增“统一刷新”按钮：立即刷新全部启用任务，使用受控并发避免请求突发；暂停、正在检查或冷却中的任务会自动跳过并提示数量。

### 0.4.69

- 监控任务新增“统一管理”栏，可批量开启/关闭旧商品更新和 Windows 弹窗，并统一设置 1/2/5/10 秒查询间隔；每项支持“保持不变”。
- 0.1 秒极速模式和 0.5 秒快速模式不支持统一切换，仍需在单个关键词中设置并遵守对应配额。

### 0.4.68

- 监控任务发生超时、请求异常、限流退避或持续错误时，任务卡片会显示红橙色边框、背景和光晕高亮；重试期间保留高亮，直到后续检查真正成功后自动恢复普通样式。

### 0.4.67

- 新增 0.1 秒极速轮询模式，并将原 0.5 秒模式调整为快速模式。
- 取消写死的单关键词限制；“个性化设置”可分别配置允许同时运行的 0.1 秒极速任务数和 0.5 秒快速任务数，范围为 0–20，暂停任务不占配额。
- 超出配额或尝试把配额降到当前运行数量以下时会阻止操作并显示明确提示；原有限流退避和访问封禁冷却继续生效。

## 2026-08-27

### 0.4.66

- 修复软件内版本号长期硬编码为 `V0.2.0` 的问题；侧边栏现在从 Electron 主进程读取当前真实版本，并在“偏好设置”中新增醒目的当前版本信息。
- 安装包：`Mercari-Pulse-0.4.66-Setup.exe`。

### 0.4.65

- 修复启用 Windows 系统代理时 QQ 商品图片无法发送：图片下载改用 Electron 网络会话，继承系统代理配置；文字消息仍优先发送，图片继续在后台异步加载与上传。
- 安装包：`Mercari-Pulse-0.4.65-Setup.exe`。

### 0.4.64

- 程序重启后会为已有监控任务执行一次静默基线同步：关机期间积累的商品只记为已见，不加入商品动态，也不触发 Windows 或 QQ 上新通知；同步完成后恢复正常实时提醒。
- 安装包：`Mercari-Pulse-0.4.64-Setup.exe`。

### 0.4.63

- 后台管理者移除监控关键词时，会同步从所有 QQ 私聊和群聊的订阅列表中仅移除该关键词，保留各用户的其他订阅，并向受影响的已启用机器人目标发送“关键词已被管理员移除”的提示。

### 0.4.62

- QQ 机器人推送新增拍卖商品提示：检测结果确认属于拍卖时，消息会明确显示“商品类型：拍卖商品”。

### 0.4.61

- 每个监控关键词新增独立的“Windows 弹窗”开关。关闭后仍会继续监控、显示商品动态并推送 QQ，仅不再显示该关键词的 Windows 右下角通知。

### 0.4.60

- 我的收藏中的已售商品新增暖红色边框与背景高亮，方便在列表中快速识别。
- 商品动态中的拍卖商品新增琥珀色边框高亮，与直售商品清晰区分。

### 0.4.59

- 排除词标题旁新增圆形“？”帮助按钮；点击可查看逗号、顿号、换行分隔规则及三种输入示例。
- 移除导致高级设置行高度不一致的内联案例文本，恢复排除词、首次展示和价格筛选的整齐对齐。

### 0.4.58

- 合并多屏蔽词支持：逗号、顿号或换行分隔的屏蔽词会被规范化，并在 Mercari 请求与本地商品动态中双重过滤。
- 新建监控和“添加屏蔽词”输入框均加入了可直接参考的使用示例。

### 0.4.57

- “关于 1 秒延迟”提示卡片已纳入主题系统，背景、边框、标题和正文会随所有主题同步切换。
- 极简白改为纯白底、纯白卡片和极浅灰分隔线，移除灰色背景渐变。

## 2026-08-26

### 0.4.56

- 个性化设置新增“曜石黑”和“极简白”两套完整主题；会同步应用背景、侧边栏、卡片、表单和按钮配色。

### 0.4.55

- 修复 QQ 私聊或群聊已存在于本地配置时，收到 `/bind 名称` 却错误提示“目标初始化中”的问题。
- QQ 会话现按机器人、会话类型与 QQ OpenID 识别；再次执行 `/bind 新名称` 会可靠地更新原有绑定名称，不会新增重复目标。

### 0.4.54

- 监控任务列表支持默认展开、折叠/展开切换，以及 1–10 条的可视高度设置；任务过多时可独立滚动，不再把商品动态挤到页面下方。
- 新增翡翠绿、深海蓝、暮光紫、玫瑰粉、琥珀金完整主题色板，主题会应用到背景、侧边栏、卡片、表单与交互控件。

### 0.4.43

- QQ 指令面板改为对已发现并启用的私聊/群聊使用 `target_type=specific` 创建，不再创建 `target_type=all` 全局面板，规避腾讯对全局面板返回的 40030013 数量限制。

### 0.4.42

- 运行日志新增“调试”级别。QQ SDK 的请求、响应与错误会直接写入持久化运行日志，不再依赖主进程控制台。

### 0.4.41

- 修复 QQ API 诊断日志未显示：将 SDK 的调试输出写入软件可见的“信息”级别运行日志。

### 0.4.40

- QQ API 调用会写入软件自带日志，面板创建失败时可查看腾讯返回的非敏感响应内容与实际限制值，用于定位新机器人也触发的配额错误。

### 0.4.39

- 恢复 QQ 指令面板，并修复重复创建问题：首次成功创建后持久化保存私聊、群聊的 `panel_id`，后续同步改为更新已有面板，不再依赖可能为空的面板列表。
- 更换 QQ AppID 时会自动丢弃旧 AppID 的面板 ID，避免跨机器人错误更新。

### 0.4.38

- 修复新建 QQ 机器人同步菜单时的“指令面板超出数量限制”：默认同步只更新官方自定义菜单，不再请求有平台配额异常的 `/v2/panels` 接口。
- `绑定 名称` 等文本指令仍然可用，可通过“监控管理”菜单或直接在聊天框发送。

### 0.4.37

- 以 0.4.36 为基线重新开发 QQ 功能，新增 `绑定 名称` 指令；可为当前私聊或群聊保存名称，支持 `绑定名称`、`绑定昵称`、`绑定群名` 同义写法。
- QQ 帮助、快捷菜单和指令面板均加入“绑定名称”入口；本版本不要求先绑定才可使用其他功能。

### 0.4.36

- QQ 推送目标会记录 QQ 事件中提供的使用者昵称，并显示在偏好设置中；手动备注不会被自动昵称覆盖。
- 每个监控关键词卡片会显示订阅它的 QQ 私聊/群聊；QQ 指令移除或清空关键词后，对应显示立即同步移除。

### 0.4.35

- 修复多关键词并发检查时的状态文件写入竞争：多个任务同时完成不会再争用同一个临时文件，避免部分关键词停止调度、约 15 秒后才被健康检查补跑。
- 状态保存改为按顺序原子写入，关键词增多时“上次成功”的间隔更稳定。

### 0.4.34

- 新增单关键词访问限制冷却：检测到 Mercari 403、429、访问拒绝或限流响应时，该关键词自动暂停 15 分钟后再恢复，不影响其他关键词。
- 监控卡片会显示“冷却中”及预计恢复时间；可手动刷新提前测试连接是否恢复。

### 0.4.33

- 新增“旧商品更新（首次发现）”处理：原始上架较早、但在监控开始后被编辑并重新进入搜索结果的商品会显示在动态中，并触发桌面通知与 QQ 推送。
- 此类卡片会明确说明其为首次发现的旧商品更新；因为没有更新前的本地快照，不会虚构价格或标题差异。

### 0.4.32

- 修复重复打开程序时创建多个窗口的问题：应用现在只允许一个后台监控进程运行；再次启动会自动恢复并聚焦已打开的主窗口。

### 0.4.31

- 合并系统代理兼容性修复：Mercari 的搜索、商品详情、店铺商品详情和图片可用性检查均改用 Electron 网络层，支持 Windows 系统代理、PAC/WPAD 与代理认证。
- Mercari 请求超时时给出明确的网络或系统代理检查提示。

### 0.4.30

- 新增软件内置“运行日志”页面，记录监控任务、首次基线、上新、旧商品更新、收藏状态变化及异常。
- 日志保留最近 500 条并写入本地状态，应用重启后仍可查看；支持按信息、警告、错误筛选。

## 2026-08-25

### 0.4.29

- 修复首次建立基线时缩略图校验瞬时失败导致“商品已记住、但一张首次卡片也不显示”的问题。
- 空基线会每 5 秒无通知重试；成功后以“初始结果”补回在售商品，不触发桌面通知或 QQ 推送。

### 0.4.28

- 首次展示严格排除已售商品；在售商品不足设定数量时不再以已售商品补足。
- 搜索结果仍会在后台用于去重，避免旧商品之后被误报为上新。

### 0.4.27

- 保留 Mercari 网页“新しい順”的原始接口排序，不再按原始创建时间二次重排。
- 修复被编辑或重新索引的旧商品在网页前排、但软件首次展示中被错误压后的问题。

### 0.4.26

- 首次展示优先选择在售商品；此策略随后在 0.4.28 收紧为完全不展示已售商品。

### 0.4.25

- 修复“添加屏蔽词”按钮无响应：改为应用内输入框，可一次输入多个词。
- 新建关键词默认开启“旧商品更新监控”。
- 收藏商品确认已售后停止后续轮询，保留已售记录供查看。

### 0.4.24

- 支持为已有关键词追加屏蔽词。
- QQ 指令新增：`关键词 添加屏蔽词 屏蔽词1、屏蔽词2`。
- QQ 机器人收到无法解析的内容时回复“指令错误 可以先在帮助中查看指令”。
- QQ 指令面板加入屏蔽词填写提示。

### 0.4.23

- 旧商品编辑现在与上新共用桌面通知及 QQ 推送通道。
- 商品动态、桌面通知与 QQ 消息会标明“旧商品更新”，并说明可识别的变化：价格、标题、在售/已售状态或主图。
- 兼容旧版仅保存更新时间的本地记录；无法精确比对时显示“卖家编辑了商品信息”。

### 0.4.22

- 为每个关键词新增“旧商品更新监控”开关。
- 保存已见商品的更新时间；检测到卖家编辑时在商品动态中标记“旧商品更新”。

### 0.4.21

- 修复关键词刚建立后，搜索索引延迟返回大量历史商品并被误报为上新的问题。
- 监控开始前已上架的延迟结果会只写入去重基线，不通知、不进入商品动态。

### 0.4.20

- 收藏监控支持 Mercari Shops 商品的实际售出状态识别。

### 0.4.19

- 防止已售旧商品因搜索结果延迟而被错误当作新上架商品推送。

### 0.4.18

- QQ 机器人可发送 Mercari Shops 商品图片。

### 0.4.17

- 首次结果支持显示已售状态（该展示规则已在 0.4.28 调整为不展示已售）。
- 新增收藏商品的价格变化与售出状态监控。

### 0.4.16

- 补充首次展示数量 1 至 5 条的自动化覆盖测试。

### 0.4.15

- 改进首次结果的排序逻辑；该实现已在 0.4.27 替换为与 Mercari 网页“新しい順”一致的接口排序。

### 0.4.14

- 无法确认拍卖属性的商品标记为“煤炉直售类”。

### 0.4.13

- 允许 Mercari Shops 商品缩略图通过校验并显示。

### 0.4.12

- 修复取消关键词并清理商品动态后，输入框可能无法继续输入的焦点锁定问题。

### 0.4.11

- 发布维护版本，整理此前商品动态保留与通知关闭操作的改动。

### 0.4.10

- 缩略图临时不可访问时不再永久丢弃新商品；后续轮询会自动重试。

### 0.4.9

- 右下角通知卡片新增关闭按钮，可立即取消而无需等待自动消失。

### 0.4.8

- 商品动态增加容量回收：每个关键词最多 200 条、全局最多 300 条。
- 全局回收优先保留每个关键词最后一条商品动态。
